export function EditorToolbar({ title, onTitle, fit, onFit }: { title: string; onTitle: (value: string) => void; fit: "cover" | "contain"; onFit: (value: "cover" | "contain") => void }) {
  return <section className="control-section"><div className="section-title"><span>03</span><div><h2>Últimos detalles</h2><p>Simple, pero personal.</p></div></div><label className="field-label">Título de la lámina<input value={title} maxLength={42} onChange={(e) => onTitle(e.target.value)} /></label><div className="fit-control"><span>Encuadre automático</span><div><button className={fit === "cover" ? "active" : ""} onClick={() => onFit("cover")}>Rellenar</button><button className={fit === "contain" ? "active" : ""} onClick={() => onFit("contain")}>Ver completa</button></div></div></section>;
}
